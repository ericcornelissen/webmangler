/**
 * @fileoverview
 * Contains constants shared between scripts.
 */

"use strict";

const TEST_TYPE_BENCHMARK = "benchmark";
const TEST_TYPE_TEST = "TEST";

module.exports = {
  TEST_TYPE_BENCHMARK,
  TEST_TYPE_TEST,
};
