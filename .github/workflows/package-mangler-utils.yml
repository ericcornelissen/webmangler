# NOTICE:
# THIS WORKFLOW IS GENERATED AND SHOULD NOT BE EDITED MANUALLY.
name: Validate package mangler-utils
on:
  push:
    branches:
      - main
    paths:
      - "packages/mangler-utils/**"
  pull_request:
    paths:
      - "packages/mangler-utils/**"
jobs:
  compile:
    name: Compile package mangler-utils
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/mangler-utils dependencies
        working-directory: ./packages/mangler-utils
        run: npm ci
      - name: Compile packages/mangler-utils
        working-directory: packages/mangler-utils
        run: npm run compile
  test:
    name: Test package mangler-utils
    runs-on: ubuntu-latest
    needs: [compile]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/mangler-utils dependencies
        working-directory: ./packages/mangler-utils
        run: npm ci
      - name: Run test suite for packages/mangler-utils
        run: npm run coverage -- mangler-utils
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          file: ./_reports/coverage/lcov.info
          flags: package-mangler-utils
