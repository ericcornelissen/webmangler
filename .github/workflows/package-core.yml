# NOTICE:
# THIS WORKFLOW IS GENERATED AND SHOULD NOT BE EDITED MANUALLY.
name: Validate package core
on:
  push:
    branches:
      - main
    paths:
      - "packages/core/**"
  pull_request:
    paths:
      - "packages/core/**"
jobs:
  benchmark:
    name: Benchmark package core
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/undefined dependencies
        working-directory: ./packages/undefined
        run: npm ci
      - name: Run benchmarks for packages/core
        run: npm run benchmark -- core
  compile:
    name: Compile package core
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/undefined dependencies
        working-directory: ./packages/undefined
        run: npm ci
      - name: Compile packages/core
        working-directory: packages/core
        run: npm run compile
  test:
    name: Test package core
    runs-on: ubuntu-latest
    needs: [compile]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/undefined dependencies
        working-directory: ./packages/undefined
        run: npm ci
      - name: Run test suite for packages/core
        run: npm run coverage -- core
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          file: ./_reports/coverage/lcov.info
          flags: package-core
