# NOTICE:
# THIS WORKFLOW IS GENERATED AND SHOULD NOT BE EDITED MANUALLY.
name: Validate package language-utils
on:
  push:
    branches:
      - main
    paths:
      - "packages/language-utils/**"
  pull_request:
    paths:
      - "packages/language-utils/**"
jobs:
  compile:
    name: Compile package language-utils
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/language-utils dependencies
        working-directory: ./packages/language-utils
        run: npm ci
      - name: Compile packages/language-utils
        working-directory: packages/language-utils
        run: npm run compile
  test:
    name: Test package language-utils
    runs-on: ubuntu-latest
    needs: [compile]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Use Node.js 15
        uses: actions/setup-node@v2
        with:
          node-version: 15.14.0
          cache: npm
      - name: Install monorepo dependencies
        working-directory: ./
        run: npm ci
      - name: Install packages/language-utils dependencies
        working-directory: ./packages/language-utils
        run: npm ci
      - name: Run test suite for packages/language-utils
        run: npm run coverage -- language-utils
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          file: ./_reports/coverage/lcov.info
          flags: package-language-utils
